<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 400 400" role="img" aria-label="Praxia">
  <title>Praxia — multi-agent orchestrator with cyclic memory</title>
  <defs>
    <linearGradient id="gold" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0" stop-color="#e9c378"/>
      <stop offset="1" stop-color="#c9a456"/>
    </linearGradient>
    <radialGradient id="bg" cx="50%" cy="20%" r="90%">
      <stop offset="0" stop-color="#1d1d28"/>
      <stop offset="0.85" stop-color="#0a0a0f"/>
    </radialGradient>
  </defs>

  <!-- Rounded-square background; safe for both square + circular crops -->
  <rect width="400" height="400" rx="88" fill="url(#bg)"/>

  <!-- 5-layer memory stack, brightest at bottom (Layer 1, raw personal memory)
       fading upward toward Layer 5 (graph / refined). Compressed to upper
       half so the wordmark has room below. -->
  <g>
    <rect x="118" y="60" width="164" height="18" rx="5" fill="url(#gold)" opacity="0.38"/>
    <rect x="118" y="90" width="164" height="18" rx="5" fill="url(#gold)" opacity="0.54"/>
    <rect x="118" y="120" width="164" height="18" rx="5" fill="url(#gold)" opacity="0.70"/>
    <rect x="118" y="150" width="164" height="18" rx="5" fill="url(#gold)" opacity="0.86"/>
    <rect x="118" y="180" width="164" height="18" rx="5" fill="url(#gold)"/>
  </g>

  <!-- Promotion / cycle arc: from Layer 1 (bottom-right) up to Layer 5 (top-right) -->
  <path d="M 286 189 C 336 189, 336 69, 286 69"
        fill="none" stroke="url(#gold)" stroke-width="5"
        stroke-linecap="round" opacity="0.9"/>
  <!-- Arrow head pointing into Layer 5 -->
  <path d="M 277 60 L 286 69 L 277 78"
        fill="none" stroke="url(#gold)" stroke-width="5"
        stroke-linecap="round" stroke-linejoin="round" opacity="0.9"/>

  <!-- Wordmark -->
  <text x="200" y="310"
        font-family="-apple-system, BlinkMacSystemFont, 'Segoe UI', Inter, Arial, sans-serif"
        font-size="76" font-weight="700"
        fill="url(#gold)" text-anchor="middle"
        letter-spacing="-1">Praxia</text>
</svg>
